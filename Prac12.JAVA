class Prac12
{
    public static void main (String[] args)
    {
       for(i=0; i<3; i++)
       {
            ch = (char) math.random()*26 + ' A ';
            s.append(ch);
       } 
       for(i=0;i<4;i++)
       {
            ch = (char) math.random()*10 + 0;
            s.append(ch)
       }
    }
}